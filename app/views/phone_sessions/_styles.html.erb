<style>
  .phone-auth {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: linear-gradient(135deg, #1a1a2e 0%, #16213e 50%, #0f3460 100%);
    padding: 1.5rem;
    font-family: 'Inter', system-ui, sans-serif;
  }

  .phone-auth__card {
    background: rgba(255, 255, 255, 0.06);
    backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 1.5rem;
    padding: 2.5rem 2rem;
    width: 100%;
    max-width: 420px;
    color: #fff;
  }

  .phone-auth__header {
    text-align: center;
    margin-bottom: 2rem;
  }

  .phone-auth__logo {
    width: 64px;
    height: 64px;
    border-radius: 16px;
    margin-bottom: 1rem;
    object-fit: cover;
  }

  .phone-auth__title {
    font-family: 'Space Grotesk', sans-serif;
    font-weight: 700;
    font-size: 1.5rem;
    margin-bottom: 0.5rem;
    color: #fff;
  }

  .phone-auth__subtitle {
    color: rgba(255, 255, 255, 0.6);
    font-size: 0.9rem;
    margin: 0;
  }

  .phone-auth__alert {
    background: rgba(180, 83, 60, 0.2);
    border: 1px solid rgba(180, 83, 60, 0.3);
    color: #f0a090;
    padding: 0.75rem 1rem;
    border-radius: 0.75rem;
    font-size: 0.85rem;
    margin-bottom: 1.5rem;
    text-align: center;
  }

  .phone-auth__form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .phone-auth__input-group {
    display: flex;
    align-items: center;
    background: rgba(255, 255, 255, 0.08);
    border: 1.5px solid rgba(255, 255, 255, 0.15);
    border-radius: 0.75rem;
    overflow: hidden;
    transition: border-color 0.2s;
  }

  .phone-auth__input-group:focus-within {
    border-color: rgba(225, 184, 74, 0.6);
    box-shadow: 0 0 0 3px rgba(225, 184, 74, 0.15);
  }

  .phone-auth__prefix {
    padding: 0.85rem 0.75rem 0.85rem 1rem;
    color: rgba(255, 255, 255, 0.5);
    font-family: 'Space Grotesk', sans-serif;
    font-weight: 600;
    font-size: 1rem;
    white-space: nowrap;
  }

  .phone-auth__input {
    flex: 1;
    background: transparent;
    border: none;
    color: #fff;
    font-size: 1rem;
    padding: 0.85rem 1rem 0.85rem 0;
    outline: none;
    font-family: 'Space Grotesk', sans-serif;
  }

  .phone-auth__input--code {
    background: rgba(255, 255, 255, 0.08);
    border: 1.5px solid rgba(255, 255, 255, 0.15);
    border-radius: 0.75rem;
    color: #fff;
    padding: 1rem;
    font-family: 'Space Grotesk', sans-serif;
    font-weight: 700;
    outline: none;
    transition: border-color 0.2s;
  }

  .phone-auth__input--code:focus {
    border-color: rgba(225, 184, 74, 0.6);
    box-shadow: 0 0 0 3px rgba(225, 184, 74, 0.15);
  }

  .phone-auth__input::placeholder {
    color: rgba(255, 255, 255, 0.3);
  }

  .phone-auth__btn {
    background: linear-gradient(135deg, #E1B84A, #d4a93c);
    color: #1a1a2e;
    border: none;
    padding: 0.85rem;
    border-radius: 0.75rem;
    font-weight: 700;
    font-size: 1rem;
    cursor: pointer;
    transition: opacity 0.15s, transform 0.1s;
    font-family: 'Space Grotesk', sans-serif;
  }

  .phone-auth__btn:hover {
    opacity: 0.92;
    transform: translateY(-1px);
    box-shadow: 0 4px 12px rgba(225, 184, 74, 0.3);
  }

  .phone-auth__features {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 0.75rem;
    margin-top: 2rem;
    padding-top: 1.5rem;
    border-top: 1px solid rgba(255, 255, 255, 0.08);
  }

  .phone-auth__feature {
    display: flex;
    align-items: center;
    gap: 0.5rem;
    color: rgba(255, 255, 255, 0.5);
    font-size: 0.8rem;
  }

  .phone-auth__feature i {
    color: #E1B84A;
    font-size: 0.9rem;
    width: 20px;
    text-align: center;
  }

  .phone-auth__footer,
  .phone-auth__hint {
    text-align: center;
    margin-top: 1.5rem;
    margin-bottom: 0;
    color: rgba(255, 255, 255, 0.4);
    font-size: 0.8rem;
  }

  .phone-auth__link {
    color: #E1B84A;
    text-decoration: none;
    font-weight: 600;
  }

  .phone-auth__link:hover {
    text-decoration: underline;
    color: #E1B84A;
  }
</style>
